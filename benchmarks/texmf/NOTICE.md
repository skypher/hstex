# Vendored benchmark input

`tex/latex/cleveref/cleveref.sty` is Toby Cubitt's cleveref 0.21.4, selected
from `/home/yang/texmf` by the pinned reference build. Its header states the
copyright and permits distribution or modification under the LaTeX Project
Public License, version 1.2 or later.

The file is vendored only to keep the benchmark input stable. It is not linked
into HSTeX and does not provide engine implementation code.
