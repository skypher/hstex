% WHAT ONE BREAK IS WEIGHED AS, IN TRIP'S FIRST PASS. -- FIXED.
%
% The fault, and the fix, are at the bottom of this file. What is above it is
% the account of the hunt, kept because most of it is what the next one of
% these will need.
%
% This was the last divergence in trip's first pass, and the whole of it. The
% two engines agreed line for line up to `@@27:' and then parted company at
% the breakpoint after `[]11-1':
%
%   ref:    @\discretionary via @@0 b=42 p=88 d=9508
%           @@28: line 1.1- t=9508 -> @@0
%           @\discretionary via @@25 b=10000 p=88 d=100007744
%           @\discretionary via @@23 b=1558 p=88 d=2436108
%           @\discretionary via @@22 b=6396 p=88 d=40916560
%
%   hstex:  @\discretionary via @@0 b=2 p=88 d=7748
%           @@28: line 1.2- t=7748 -> @@0
%           @\discretionary via @@25 b=10000 p=88 d=100007744
%           @\discretionary via @@22 b=12 p=88 d=7888
%
% ONE NUMBER, AND EVERYTHING ELSE FOLLOWS FROM IT. The demerits agree given
% the badness -- d = b^2 + p^2, trip having \linepenalty nought, and 42^2 +
% 88^2 = 9508 exactly, 2^2 + 88^2 = 7748 exactly -- so nothing is wrong with
% the demerit arithmetic. The fitness class follows from the badness too: 42
% is loose and 2 is decent, which is `line 1.1-' against `line 1.2-'. And the
% breaks HSTeX then leaves out, `via @@23' and `via @@24' and the two
% `@\par via' that follow them, are the ones that would have been reached
% through a class it never made. One badness is the whole fault.
%
% HOW MUCH IT MATTERS -- AND THE ANSWER `LESS THAN IT LOOKS' WAS WRONG.
% It was recorded here that the `\showbox0' trip draws afterwards agrees box
% for box, and therefore that the two engines set the same material and
% differ only over what they considered on the way. THAT DOES NOT FOLLOW.
% trip line 69 sets `\showboxbreadth 55', and the paragraph in question has
% 182 nodes: the display stops at `....etc.' after about 55 items of that
% hbox, and the discretionary whose weighing diverges is node 112 -- well
% past the cut. The two displays are identical AS FAR AS THEY GO and say
% nothing whatever about the rest. The eighteen diagnostics do still agree,
% and the second pass does still read the same format, so the divergence is
% bounded; but it is NOT established to be trace-only, and it should not be
% assumed to be.
%
% WHAT THE BADNESS IS WORTH, MEASURED. Instrumenting the trial line shows
% every other component of it is nought -- no background, no entry width
% from a previous break, no kerns -- so the width is the material plus the
% discretionary's pre-break text. This engine makes that 65531 against a
% goal of 0, so the line must SHRINK: decent, b=2. The reference's fitness
% class says it stretches, since a shrinking line can only come out tight or
% decent and never loose, and `line 1.1-' is loose.
%
% The size of the difference falls out of two breaks through the same
% discretionary. The reference weighs one of them at b=6396, which pins its
% width to -524288 exactly; this engine has -65538 there, so the difference
% is -458750 sp, or 7pt less two scaled points. Carrying that same figure to
% the break `via @@0' -- where this engine has 65531 -- gives -393219, and
% glue_badness(393219, 524156) is 42 EXACTLY, which is what the reference
% draws. One number explains both.
%
% AND THEN IT WAS SEEN. `\showboxbreadth' is a DISPLAY setting and changes
% no typesetting at all, so raising it in a copy of trip.tex shows the rest
% of that box without disturbing what is in it:
%
%   sed '69s/\showboxbreadth 55 /\showboxbreadth 9999 /' trip.tex
%
% and running both engines over the copy. The material differs. Both draw
% FIVE discretionaries and the first four are identical -- replacing 7, 6, 1
% and 10, each with the same texts. The fifth is not:
%
%   ref:    ....\kern-2.00002
%           ....\discretionary replacing 2
%           .....\rip -
%           ....|\rip 5 (ligature |1)
%           ....|\rip 7 (ligature |)
%           ....\rip 1
%           ....\rip 7 (ligature |)
%
%   hstex:  ....\kern-2.00002
%           ....\discretionary replacing 67
%           .....\rip 1
%           .....\rip 1 (ligature -)
%           .....\rip 7 (ligature |)
%           ....|\rip 5 (ligature |1)
%           ....|\rip 7 (ligature |)
%           ....\rip 1                      <- and 66 more
%
% Same break, same post-break text, and the two runs END at the same node.
% They differ over where the run BEGINS: the reference's starts 65 nodes
% later and replaces 2 where this engine replaces 67 -- 67 - 2 = 65 exactly.
% And the reference's pre-break is the hyphen ON ITS OWN where this engine
% sets the letter in front of the break again together with it.
%
% THAT IS THE SAME FAULT AS what-a-word-hyphenates-to-when-the-hyphen-is-
% not-there.tex, which was fixed for the case of TWO breaks in a word: the
% second one's letter had been taken into the first one's run, so only the
% hyphen was left to put in front of the break. Here there is ONE break and
% no earlier discretionary to have taken anything, and the reference still
% puts the hyphen in alone. So the rule `fresh = previous >= settled_item'
% is not the whole of it: what decides it is not whether an earlier
% discretionary consumed the letter but where the unbroken development has
% got to when the break is reached. Still open, and this is where to start.
%
% THE WORD, MEASURED. Instrumented, it is `111' -- three letters at nodes
% 43, 108 and 173, ending at 175, with breaks wanted at letters 1 and 2 and
% \lefthyphenmin and \righthyphenmin both 1. Taking the word's own base at
% 43, letter 0 stands at 0, letter 1 at 65, letter 2 at 130, and the whole
% run is 132 nodes. This engine suppresses the break at letter 1 -- a word
% set again without a left boundary begins with its first two letters in one
% run -- so ONE discretionary is drawn, and the reference draws one too.
%
% A RULE THAT LOOKED RIGHT AND IS NOT. Where a run ENDS was fixed by matching
% the two branches from the back on letter boundaries; the obvious guess is
% that where it BEGINS is the same question asked from the front -- match the
% text set again in front of the break against what is already there, share
% the common prefix, and begin the run where they part. On paper it accounts
% for all three cases known:
%
%   - trip's break at letter 2: letter 1's 65 nodes are unchanged by the
%     break, so all 65 are shared, the run begins at 130 and replaces 2, and
%     the hyphen stands alone in front of the break.
%   - the probe word's first break: letter 0 gains a right boundary at the
%     end of a line, so the two part at once, nothing is shared, and the
%     letter goes into the pre-break text.
%   - the probe word's second break: as trip's.
%
% Written and measured, it does neither. trip's fifth discretionary does not
% move, and the fourth -- `replacing 10', which was right -- becomes 9.
%
% WHY, AND THIS IS THE THING WORTH KNOWING. The prefix cannot be seen because
% this engine never builds it. Setting letter 1 again together with the
% hyphen gives THREE nodes here -- `1', `1 (ligature -)', `7 (ligature |)' --
% where the letter's own material in the run is SIXTY-FIVE, the rest of them
% put there by the font's ligature program running across the pair of letters
% it stands between. Reconstituting the letter on its own does not produce
% them, so there is no 65-node prefix to match and no way for the run to
% begin at 130.
%
% So the difference is not in the bookkeeping around the reconstitution but
% in the reconstitution itself: the reference develops the branch in front of
% a break from the state the unbroken run is already in, and this engine sets
% the letter again in isolation. That is the next thing to measure, and it is
% a bigger change than either of the two made today.
%
% WHERE IT IS NOT: not the line width, which is nought for both (\hsize is
% at its INITEX default through trip's first pass, and a wrong goal would
% move every break's badness rather than this one's). Not the material
% before node 33, since the two breaks that pin the figure start at 0 and at
% 33 and show the SAME difference. Not the first 55 items, which the box
% display does cover and which agree. What is left is nodes 56 to 112 and
% the pre-break text of the discretionary at 112 -- which this engine builds
% as `1', `1', `7', every one of them nought wide.
%
% WHAT IT IS NOT. Each of these was written down and found to agree exactly,
% in trip's own font, at line widths where the badness comes out finite:
%
%   - a box in the line before the word that breaks;
%   - a hyphenation discretionary, with \hyphenchar a character the font has
%     and one it has not;
%   - interword glue with stretch, over four line widths from 40pt to 120pt;
%   - the paragraph inside a \valign entry, which is where trip's is;
%   - a negative \hskip at the head of the paragraph, which trip has;
%   - \looseness, which trip's \everypar sets;
%   - \emergencystretch, which trip sets.
%
% BISECTED, AND IT TAKES TWO THINGS. trip.tex's own first 76 lines followed
% by
%
%   \hskip-9pt\righthyphenmin0
%   1Z1pts\patterns{q9q} -\0qq \showlists{\language\?\noboundary111}%
%   \cr}}\showbox0}
%   \fi\end
%
% still diverges, in the same place and by the same numbers. Cutting it
% further says which two ingredients carry it, each necessary and neither
% sufficient:
%
%   - `\righthyphenmin0'. Put back to 2 or more, or left out so that it keeps
%     the 1000000000 of trip line 68, the two engines agree exactly.
%   - `\language\?' inside the group after \showlists -- a language change
%     PART WAY THROUGH the paragraph. Take it out and they agree exactly.
%
% Everything else in those lines can go: \showlists itself, the braces round
% the group, \noboundary, `\patterns{q9q}' given mid-line,
% `\hyphenchar\rip=`-', \setlanguage, \unhbox0, `7A', and the whole of trip's
% line 79 bar the \cr. Only `\hskip-9pt' changes anything, and only the size
% of the difference -- 40 differing trace lines with it, 16 without.
%
% So it is a language changed mid-paragraph together with a \righthyphenmin
% that the paragraph did not begin with. What that pair does is decide which
% words may be hyphenated where: a language whatsit carries the two
% hyphenmins as they stand when it is made, and the words in front of the
% first one are hyphenated by what the paragraph BEGAN with. Written out on
% its own -- a paragraph entered with \righthyphenmin 1000000000, a word, then
% `\righthyphenmin0 \language5', then another word -- the two engines agree,
% so the pair alone is not the whole of it either. What is left between that
% and trip is the rest of trip's state: two languages whose patterns differ,
% \lccode`A=1 and \lccode 1 = 49 from line 71-72, and \patterns given for
% language 255 and for 256.
%
% (The reproduction above is a recipe rather than a file: trip.tex is fetched
% and pinned, never vendored, so nothing here may carry its lines. NOTE: that
% recipe has since DRIFTED and no longer draws this fault -- under it the two
% engines now differ at a penalty of 89 rather than 88, which is a different
% discretionary. It needs deriving again before it is trusted.)
%
% WHAT IS DRAWN HERE is the set that agrees, kept so that the next attempt
% starts past it rather than at the diff again.
\catcode`\{=1 \catcode`\}=2 \catcode`\#=6 \catcode`\&=4
\tracingonline=1 \tracingparagraphs=1
\font\trip=trip \trip
\parindent=0pt \pretolerance=-1 \tolerance=10000 \hbadness=10000
\hyphenchar\trip=`1
\lccode`1=`1 \lccode`A=`A \lccode`B=`B
\patterns{1A1 A1B}
\setbox0=\hbox to 7pt{}
\message{[A a box before the word that breaks, over four widths]}
\hsize=40pt \noindent \copy0 1AB 1AB 1AB\par
\hsize=60pt \noindent \copy0 1AB 1AB 1AB\par
\hsize=80pt \noindent \copy0 1AB 1AB 1AB\par
\hsize=120pt \noindent \copy0 1AB 1AB 1AB\par
\message{[B a hyphen character the font has not got]}
\hsize=60pt \hyphenchar\trip=200 \noindent \copy0 1AB 1AB 1AB\par
\hyphenchar\trip=`1
\message{[C inside a valign entry, with a negative hskip at its head]}
\setbox1=\hbox{\valign{#\cr\hskip-9pt 1AB 1AB \copy0 1AB1AB\cr}}
\message{[D the same under looseness]}
\setbox1=\hbox{\valign{#\cr\looseness-1 \hskip-9pt 1AB 1AB \copy0 1AB1AB\cr}}
\message{[E and with emergencystretch]}
\emergencystretch=9pt
\setbox1=\hbox{\valign{#\cr\hskip-9pt 1AB 1AB \copy0 1AB1AB\cr}}
\emergencystretch=0pt
\message{[done]}
\end

% ============================================================================
% FIXED, and it was none of the things above.
%
% A word set again WITHOUT A LEFT BOUNDARY begins with its first two letters
% in one run, so a break between them cannot be taken. This engine knew that
% and dropped the break. What it did not do was carry the run those two
% letters make: the NEXT break in the word went on beginning its own run at
% its own letter, sixty-five nodes early, replacing 67 nodes where the
% reference replaces 2 and setting the letter in front of the break again
% where the reference puts in the hyphen alone.
%
% A lost break leaves no discretionary behind, but it does leave the
% development standing at the letter after it. Four lines where the break is
% dropped now say so, and trip's first pass comes out clean: 55 differing
% lines to 15, and every one of the 15 is the pdfTeX banner or the
% end-of-run statistics. Every `@' line agrees.
%
% The badness followed from it exactly as the arithmetic above said it would.
% The line was a pointful too wide, so it had to shrink where the reference
% had to stretch, and decent came out where loose belonged.
%
% Locked by where-a-run-a-lost-break-made-reaches-to.tex, which draws 26
% differing lines without the fix.
